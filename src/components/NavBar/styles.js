import { makeStyles } from '@mui/material';

export default makeStyles(() => ({
  toolbar: {
    height: '80px',
    display: 'flex',
    justifyContent: 'space-between',
    marginLeft: '240px',
  },

}));
