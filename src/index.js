import React from 'react';
import ReactDom from 'react-dom'; // this connect to the app to the public index.html

import App from './components/App';

ReactDom.render(<App />, document.getElementById('root'));
